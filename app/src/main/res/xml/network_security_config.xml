<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Remote sync uses cleartext WebSocket to LAN host IPs discovered at runtime. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
